{
    "name": "todolist",
    "private": "true",
    "version": "1.0.0",
    "description": "Sistema de gerenciamento de tarefas",
    "workspaces":[
        "apps/*"
    ],
    "scripts": {
        "api:dev": "dotenv -- yarn workspace todolist-api dev",
        "web": "yarn workspace todolist-web ng serve"
    },
    "devDependencies": {
        "dotenv-cli": "^11.0.0"
    }
}